namespace GeradorDeLabirintos.Algorithms
{
    public struct Posicao
    {
        public int Linha;
        public int Coluna;

        public Posicao(int linha, int coluna)
        {
            Linha = linha;
            Coluna = coluna;
        }
    }
}